---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";
import { getCollection } from "astro:content";

const posts = (await getCollection('blog')).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

const last_posts = posts.slice(0, 3);
---

<BaseLayout>

    <div class="pb-12 mt-5">
      <div class="text-xl py-1">Hey there 👋</div>
      <div class="text-5xl font-bold">I'm Semih Buğra Sezer</div>
      <div class="text-3xl py-3 font-bold">Computer Engineer Student</div>
      <div class="py-2">
        <text class="text-lg">
          Always trying to become a better version of myself. I've been interested in software since childhood. I'm a 2nd year computer engineering student.
          </text
        >
      </div>
      <div class="mt-8">
        <a class="btn" href="https://linkedin.com/in/semihbugrasezer" target="_blank"> Let's connect!</a>
        <a href="https://github.com/semihbugrasezer" target="_blank" class="btn btn-outline ml-5"> My github page </a>
      </div>
    </div>

    <div>
      <div class="text-3xl w-full font-bold mb-2">My last projects {"</>"}</div>
    </div>

    <HorizontalCard
    title="Av. Ebru Saylır"
    img="/proje1.png"
    desc="Sizlere hukuki danışmanlık hizmeti sunuyoruz Her türlü hukuki danışmanlık ve avukatlık hizmetiniz için Av. Ebru Sayılır Ankara Avukat'lık.."
    url="https://ebrusayilir.av.tr/"
    badge="NEW"
    />
    <div class="divider my-0"></div>
    <HorizontalCard
    title="ApkWeben"
    img="/proje2.png"
    desc="We are ApkWeben. You might be reading the most boring lines in your life right now, but don't mind me. Get All APK, Premium APK and original mobile game apps ..."
    url="https://www.apkweben.com/"
  />
    <div class="divider my-0"></div>
    <HorizontalCard
    title="My Personal Website"
    img="/proje3.png"
    desc="My personal website is a virtual space where I showcase my interests, experiences, and professional accomplishments."
    url="https://www.semihbugrasezer.com.tr/"
    badge="HOT"
  />


    <div>
      <div class="text-3xl w-full font-bold mb-5 mt-10">Latest from blog</div>
    </div>

    {
      last_posts.map((post) => (
        <HorizontalCard
          title={post.data.title}
          img={post.data.heroImage}
          desc={post.data.description}
          url={"/blog/" + post.slug}
          target="_self"
        />
        <div class="divider my-0"></div>
      ))
    }

</BaseLayout>
